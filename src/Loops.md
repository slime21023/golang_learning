# Loops

```golang
for i := 0; i < 10; i++ {
    fmt.Println(i)
}
```

### `for CONDITION`
There is no `while` loop in golang.

```golang
height := 1
for height < 5 {
    fmt.Println("still growing! current height:", height)
    height++
}
```
